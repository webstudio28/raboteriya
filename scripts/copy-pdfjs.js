/**
 * Postinstall: copy or prepare PDF.js (or other vendor assets).
 * Runs automatically after npm install.
 */
// Placeholder — add copy logic when PDF.js is needed
console.log('postinstall: copy-pdfjs.js (no-op for now)');
